language: node_js
node_js:
  - node
cache:
  directories:
    - node_modules
install:
  - npm install -g firebase-tools
script:
  - npm run build
after_success:
  - firebase deploy --project chatproject-c2766 --token $FIREBASE_TOKEN
